{
  "name": "ecommerce",
  "version": "1.0.0",
  "description": "Black Bag an ecommerce website.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "prebuild": "npm ci --workspaces",
    "build": "npm run build --workspace=view",
    "build:all": "npm run build --workspace=view && echo 'Server ready'",
    "dev:server": "npm run dev --workspace=server",
    "dev:view": "npm run dev --workspace=view",
    "install-server": "npm install --workspace=server"
  },
  "engines": {
    "node": ">=18 <23"
  },
  "author": "Suraj Aswal",
  "license": "ISC",
  "workspaces": [
    "server",
    "view"
  ]
}
